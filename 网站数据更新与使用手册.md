# 网站数据更新与使用手册

这份手册面向网站日常维护。正常更新只需要编辑 `data/` 目录中的 JSON 文件，不需要修改 HTML 或渲染脚本。

## 一、更新前准备

1. 使用 GitHub Desktop 或其他 Git 工具打开本仓库。
2. 修改前先拉取最新版本，避免覆盖已经发布的内容。
3. 使用支持 UTF-8 的编辑器打开 JSON 文件，例如 VS Code、Notepad++ 或 Sublime Text。
4. JSON 对逗号、双引号和括号要求严格。每次修改后先在本地预览。

本地预览不能完全通过双击 HTML 完成：页面使用 `fetch()` 读取 JSON 和 Markdown，而浏览器会阻止 `file://` 页面跨文件读取资源。这不是 GitHub Pages 的问题，而是浏览器的安全策略。Windows 用户可直接双击仓库里的 `预览网站.bat`，不需要手动输入命令；它本质上仍会启动一个仅供本机访问的静态服务器。

## 二、数据文件对应关系

| 文件 | 用途 | 会影响的页面 |
| --- | --- | --- |
| `data/site.json` | 姓名、头衔、邮箱、办公信息、首页更新、站点设置 | 首页、个人信息、联系、所有页脚 |
| `data/courses.json` | 课程、学期、简介、课程资源、每周资料 | 首页、课程列表、课程详情 |
| `data/research.json` | 研究方向、代表性成果、科研合作区 | 科研页 |
| `data/resources.json` | 公共讲义、课程资源、视频、阅读入口 | 资源页 |

## 三、更新教师信息

编辑 `data/site.json` 中的 `teacher` 对象：

```json
{
  "name": "郭畅（Sing）",
  "initials": "Sing",
  "title": "高校教师 · 人工智能安全 · 人工智能",
  "email": "your-email@example.com",
  "office": "九教616",
  "officeHours": "周三 13:00—14:00，或提前邮件预约"
}
```

修改后，首页、个人信息页、联系页以及页脚会自动同步。头像路径和版权年份位于同文件的 `site` 对象中。

## 四、新增或修改课程

编辑 `data/courses.json`。每门课程必须有唯一的 `id`，它会用于课程详情地址：

```text
courses/course.html?id=algorithm-java
```

最小课程结构如下：

```json
{
  "id": "new-course",
  "year": 2026,
  "term": "fall",
  "title": "新课程名称",
  "description": "课程简介",
  "resources": {
    "slides": "https://example.com/slides",
    "resources": "https://github.com/example/repo-or-reading",
    "assignment": "https://example.com/assignment",
    "video": "https://example.com/video"
  },
  "weeks": []
}
```

支持的 `term` 值为 `spring`、`summer`、`fall`、`winter`，页面会自动显示为对应英文学期。也可以直接使用 `semester` 字段，例如 `"semester": "2026 · Fall"`。

### 新增一周资料

在课程的 `weeks` 数组末尾增加对象：

```json
{
  "no": 8,
  "title": "动态规划",
  "text": "最优子结构、重叠子问题与状态转移。",
  "resources": {
    "slides": "https://example.com/week08-slides.pdf",
    "resources": "algorithm-java/week08.md",
    "assignment": "https://example.com/week08-homework.pdf",
    "video": "https://example.com/week08-video"
  }
}
```

周次资源键建议统一使用：`slides`（讲义）、`resources`（代码、论文、阅读材料或实验资料）、`assignment`（作业）、`video`（视频）、`experiment`（实验）。没有链接的字段可以删除，页面不会显示空按钮。

资源链接既可以是网络地址，也可以是仓库内的相对路径。课程详情页地址是 `courses/course.html?id=课程ID`，因此如果文件放在：

```text
courses/algorithm-java/week08.md
```

对应的数据写成：

```json
"resources": "algorithm-java/week08.md"
```

这样点击“资源”就会打开本地 Markdown 文件。

当前版本已经内置 Markdown 阅读页：课程页面中的本地 `.md` 链接会自动跳转到 `courses/markdown.html`，并在网页中排版标题、段落、列表、链接、引用、表格和代码块。通常不需要手写阅读页地址，课程页会自动生成正确的文件参数。Markdown 文件可以选择使用 YAML frontmatter，例如：

```markdown
---
title: 动态规划
week: 8
date: 2026-10-20
---

# 动态规划

正文从这里开始。
```

frontmatter 会被阅读页作为元数据忽略，不会直接显示在正文中。

> 注意：浏览器直接双击 `courses/course.html` 时，仍会阻止页面读取 JSON 和 Markdown，这是浏览器对 `file://` 页面的安全限制。部署到 GitHub Pages 后，所有文件通过 HTTPS 同源访问，课程数据和 Markdown 阅读页会正常工作。仓库中的 `.nojekyll` 文件会让 GitHub Pages 直接提供这些 JSON / Markdown 文件，而不是让 Jekyll 改写它们。

## 五、更新首页最近更新

编辑 `data/site.json` 中的 `updates` 数组。最新内容放在数组最前面：

```json
{
  "date": "2026.10.20",
  "title": "Week 08 资料已发布",
  "text": "讲义、代码和作业已经加入课程详情页。"
}
```

## 六、更新科研方向和成果

编辑 `data/research.json`：

- `topics`：研究方向卡片，`id` 必须唯一。
- `publications`：成果列表，建议按年份从新到旧排列。
- `sample`：测试数据设为 `true` 时显示“测试数据”标签，正式成果请删除该字段或设为 `false`。
- `links`：成果链接数组，每项包含 `label` 和 `url`。

研究方向卡片的“研究成果”按钮会按 `id` 自动筛选对应成果，无需额外修改脚本。

## 七、更新公共资源

编辑 `data/resources.json`，每个资源卡片包含：

```json
{
  "tag": "CODE",
  "title": "课程资源",
  "description": "课堂示例、代码仓库、论文和参考实现。",
  "url": "https://github.com/example/repo",
  "label": "打开代码链接 →"
}
```

数组顺序就是资源页的显示顺序。

## 八、本地检查

在仓库根目录运行：

```bash
python3 scripts/validate-data.py
python3 -m http.server 8080
```

校验脚本会检查 JSON 语法、必填字段、课程 ID 是否重复以及资源链接格式。

浏览器检查以下地址：

```text
http://127.0.0.1:8080/
http://127.0.0.1:8080/courses.html
http://127.0.0.1:8080/courses/course.html?id=algorithm-java
http://127.0.0.1:8080/research.html
http://127.0.0.1:8080/resources.html
```

重点确认：课程卡片出现、课程详情周次出现、科研筛选可用、资源链接可点击、邮箱链接正确。如果页面显示“资料整理中”，先打开浏览器开发者工具查看 JSON 请求是否返回 404 或 JSON 语法错误。

## 九、发布到网站

确认本地页面无误后：

1. 在 GitHub Desktop 查看变更，确认只包含预期文件。
2. 填写 commit 信息，例如 `更新算法课程 Week 08 资料`。
3. Commit 到当前分支并 Push origin。
4. 等待 GitHub Pages 构建完成，再用无痕窗口检查线上页面。

## 十、常见问题

### 页面没有更新

先强制刷新浏览器（Windows/Linux：`Ctrl + F5`，macOS：`Cmd + Shift + R`）。如果线上仍旧没有更新，检查 GitHub Pages 是否已完成构建。

### 页面显示“网站数据加载失败”

不要直接双击 HTML 文件。必须通过静态服务器访问，因为浏览器默认禁止 `file://` 页面读取 JSON。

### 新课程链接打不开

确认 `id` 只使用字母、数字和短横线，并且课程详情地址中的 `id` 与 JSON 完全一致。新增课程不需要新建 HTML 文件。

### JSON 报错

常见原因是最后一个字段后多了逗号、使用了中文引号，或者字符串中包含未转义的双引号。使用 `python -m json.tool 文件名` 可以快速定位错误。

## 十一、维护原则

- 课程、科研和资源内容放 `data/`，不要写回 HTML。
- 大文件和视频放外部平台，网站只保存链接。
- `id` 建立后尽量不要修改，否则旧的课程详情地址会失效。
- 正式科研成果发布前，记得删除 `sample` 测试标记并替换 `#` 占位链接。
- 每次发布前保留一次清晰的 Git commit，方便回退和查找历史版本。
